import * as React from 'react'

/**
 * The app marshaller component responsible for presenting the adequate UI to the user.
 */
export default class AppMarshaller extends React.Component {
    render() {
        return (

        )
    }
}